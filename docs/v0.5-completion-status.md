# v0.8 completion status

This document records the carried-over work and the v0.8 package/runtime verification work. Items marked pending remain future work.

| Item | Status | Notes |
|---|---|---|
| Property tests for priority rules | pending | Fast-check property suites remain to be added. |
| Ruby/Java/Rust/C# rule families | pending | Coverage remains planned. |
| Full adversarial fixtures | partial | Adversarial directory exists, but broad per-rule coverage remains pending. |
| Context-aware detection | pending | All current rules remain at regex tier. |
| AST tier and parser bindings | pending | Parser references are documented; no parser is invoked. |
| Runtime monitor CLI and equivalents | partial | NDJSON monitor CLI is shipped for tool-call events; broader runtime rule coverage remains pending. |
| runtime-detectable coverage entries | partial | Tool-call runtime detection is tested; coverage is intentionally narrow. |
| Clean packed-artifact testing | implemented | `npm run verify:package` packs, installs, validates, tests, scans, and checks monitor help in a clean directory. |
| Actual GitHub Packages publish | recorded | Package: `@yxin530/agent-security-engine@0.6.2`; registry: `https://npm.pkg.github.com`; repository package page: `https://github.com/yxin530/agent_security/packages`. |
| CI verification against installed package | implemented | `.github/workflows/package-verify.yml` supports manual version verification and successful tagged CI runs. |
