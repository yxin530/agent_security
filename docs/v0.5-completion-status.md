# v0.5 completion status

This document records the carried-over v0.5 work. Items marked pending are not claimed as complete by v0.6.

| Item | Status | Notes |
|---|---|---|
| Property tests for priority rules | pending | Fast-check property suites remain to be added. |
| Ruby/Java/Rust/C# rule families | pending | Coverage remains planned. |
| Full adversarial fixtures | partial | Adversarial directory exists, but broad per-rule coverage remains pending. |
| Context-aware detection | pending | All current rules remain at regex tier. |
| AST tier and parser bindings | pending | Parser references are documented; no parser is invoked. |
| Runtime monitor CLI and equivalents | partial | Opt-in runtime library exists; a complete CLI and runtime rule catalog remain pending. |
| runtime-detectable coverage entries | pending | No threat-type is currently claimed as runtime-detectable. |
| Clean packed-artifact testing | pending | Package dry-run exists; clean installed full-suite verification remains. |
| Actual npm publish verification | pending | No registry publish is claimed here. |
| CI verification against installed package | pending | CI still needs a clean installed-package test job. |
