const assert = require('node:assert/strict'); const path = require('node:path'); const { spawnSync } = require('node:child_process'); const test = require('node:test');
test('MCP smoke verification reports successful handshake and list_rules', () => { const result = spawnSync(process.execPath, [path.resolve(__dirname, '../../scripts/mcp-package-smoke.js'), path.resolve(__dirname, '../..')], { encoding: 'utf8' }); assert.equal(result.status, 0, result.stderr); assert.match(result.stdout, /MCP handshake completed/); assert.match(result.stdout, /list_rules response valid/); });
