const assert = require('node:assert/strict'); const fs = require('node:fs'); const os = require('node:os'); const path = require('node:path'); const test = require('node:test');
const { scanDetailed } = require('../../dist/engine/scan');
test('timed scan returns partial findings deterministically', () => { const root = fs.mkdtempSync(path.join(os.tmpdir(), 'agent-security-timeout-')); fs.writeFileSync(path.join(root, 'a.ts'), 'const password = "FAKE-partial-secret";'); fs.writeFileSync(path.join(root, 'b.ts'), 'const password = "FAKE-later-secret";'); let calls = 0; const result = scanDetailed(path.resolve(__dirname, '../../rules'), root, { timeoutMs: 1, now: () => (++calls > 50 ? 2 : 0) }); assert.equal(result.truncated, true); assert.ok(result.findings.length > 0); assert.ok(result.findings.length < 2); });
