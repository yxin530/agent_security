# Adversarial fixtures

This directory is reserved for edge-case fixtures used to measure false positives and false negatives separately from baseline rule fixtures. v0.5 begins the measurement process; rules remain at the regex tier until evidence supports escalation.
